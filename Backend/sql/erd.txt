The provided schema describes a database structure with multiple tables, each containing specific columns and relationships. Here is a detailed description of each table:

### SKU Table
- **Columns:**
  - `code`: A string field used to store the SKU code.
  - `name`: A string field for the SKU name.
- **Relationships:** None

### User Manager Map Table
- **Columns:**
  - `id`: An integer serving as the unique identifier.
  - `userid`: A string field referencing a user ID.
  - `managerid`: A string field referencing a manager ID.
- **Relationships:**
  - `userid` links to the `id` column in the `UserDetails` table.
  - `managerid` links to the `id` column in the `UserDetails` table.

### Role Table
- **Columns:**
  - `id`: An integer serving as the unique identifier.
  - `name`: A string field for the role name.
- **Relationships:** None

### HQ Table
- **Columns:**
  - `id`: A string field used as the unique identifier.
  - `name`: A string field for the HQ name.
- **Relationships:** None

### Stockist Table
- **Columns:**
  - `id`: A string field used as the unique identifier.
  - `name`: A string field for the stockist name.
- **Relationships:** None

### Brand Table
- **Columns:**
  - `id`: An integer serving as the unique identifier.
  - `name`: A string field for the brand name.
  - `category`: A string field for the brand category.
- **Relationships:** None

### Brand SKU Map Table
- **Columns:**
  - `brand_id`: An integer referencing the brand ID.
  - `sku_code`: A string field linking to an SKU code.
- **Relationships:**
  - `brand_id` connects to the `id` column in the `Brand` table.
  - `sku_code` links to the `code` column in the `SKU` table.

### UserDetails Table
- **Columns:**
  - `roleid`: An integer for role identification.
  - `loginenabled`: A boolean indicating if login is enabled.
  - `hq_id`: A string referencing the HQ ID.
  - `id`: A string field serving as the unique user identifier.
  - `password`: A string field for storing passwords.
  - `emp_code`: A string for employee code.
  - `name`: A string for the user's name.
- **Relationships:**
  - `roleid` links to the `id` column in the `Role` table.
  - `hq_id` links to the `id` column in the `HQ` table.

### Division HQ Map Table
- **Columns:**
  - `id`: An integer serving as the unique identifier.
  - `division_code`: A string field linking to a division.
  - `hq_id`: A string referencing an HQ ID.
- **Relationships:**
  - `division_code` links to the `code` column in the `Division` table.
  - `hq_id` links to the `id` column in the `HQ` table.

### Division Table
- **Columns:**
  - `code`: A string field for the division code.
  - `name`: A string field for the division name.
- **Relationships:** None

### Sales Table
- **Columns:**
  - `id`: An integer serving as the unique identifier.
  - `transaction_date`: A date for transaction.
  - `primary_sales`: A numeric field for sales value.
  - `primary_units`: An integer for units sold.
  - `sku_code`: A string field linking to an SKU code.
  - `division_code`: A string referencing a division code.
  - `billing_type`: A string indicating billing type.
  - `status`: A string indicating the status of the transaction.
  - `stockist_id`: A string linking to a stockist.
  - `hq_id`: A string referencing an HQ ID.
- **Relationships:**
  - `stockist_id` links to the `id` column in the `Stockist` table.
  - `sku_code` links to the `code` column in the `SKU` table.
  - `division_code` links to the `code` column in the `Division` table.
  - `hq_id` links to the `id` column in the `HQ` table.

### Target Table
- **Columns:**
  - `target_date`: A date for the target date.
  - `id`: An integer serving as the unique identifier.
  - `target_units`: An integer indicating target units.
  - `target_value`: A numeric field for target value.
  - `division_code`: A string linking to a division.
  - `hq_id`: A string referencing an HQ ID.  
  - `sku_code`: A string linking to an SKU code.
- **Relationships:**
  - `division_code` links to the `code` column in the `Division` table.
  - `hq_id` links to the `id` column in the `HQ` table.
  - `sku_code` links to the `code` column in the `SKU` table.

### Stockist HQ Map Table
- **Columns:**
  - `id`: An integer serving as the unique identifier.
  - `hq_id`: A string referencing an HQ ID.
  - `stockist_id`: A string linking to a stockist.
- **Relationships:**
  - `hq_id` links to the `id` column in the `HQ` table.
  - `stockist_id` links to the `id` column in the `Stockist` table.

### Target 3 Table
- **Columns:**
  - `target_date`: A date for the target.
  - `id`: An integer serving as the unique identifier.
  - `target_units`: An integer indicating target units.
  - `target_value`: A numeric field for the target value.
  - `division_code`: A string linking to a division.
  - `hq_id`: A string referencing an HQ ID.
  - `sku_code`: A string linking to an SKU code.
- **Relationships:** None

### Conversation Log Table
- **Columns:**
  - `id`: An integer serving as the unique identifier.
  - `timestamp`: A timestamp with time zone for logging.
  - `user_id`: A string field for the user's ID.
  - `userquery`: A text field for the user's query.
  - `sqlquery`: A text field for the SQL query generated.
- **Relationships:** None

This schema illustrates a structured database designed to maintain records related to products, organizational roles, sales, targets, and conversational logs within an organization, with specific relationships connecting various data points across the tables.